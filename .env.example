# DolphinScheduler 部署工具环境变量配置
# 复制此文件为 .env 并填写实际值

# ================================================================================
# 堡垒机配置
# ================================================================================
BASTION_HOST=1.2.3.4  # 请填写实际的堡垒机IP地址
BASTION_USER=ec2-user
BASTION_KEY=  # 使用公钥认证时留空，使用密钥文件时填写路径

# ================================================================================
# 项目配置
# ================================================================================
REMOTE_PROJECT_DIR=/home/ec2-user/dolphinscheduler-ec2-on-aws
CONFIG_FILE=config.yaml
GITHUB_BRANCH=main

# ================================================================================
# AWS 配置（可选，如果堡垒机上没有配置）
# ================================================================================
AWS_DEFAULT_REGION=us-east-2
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# ================================================================================
# SSH 配置（可选）
# ================================================================================
SSH_KEY_PATH=/path/to/your-ec2-key.pem

# ================================================================================
# 日志配置（可选）
# ================================================================================
LOG_LEVEL=INFO
DS_VERSION=3.3.2

# ================================================================================
# 使用说明
# ================================================================================
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填写实际的配置值
# 3. 运行脚本: ./deploy_and_test.sh "修复描述"
#
# 注意事项:
# - BASTION_HOST: 你的堡垒机公网IP地址
# - BASTION_KEY: 使用公钥认证时留空，使用密钥文件时填写完整路径
# - REMOTE_PROJECT_DIR: 堡垒机上项目的完整路径
# - CONFIG_FILE: 要使用的配置文件名
#
# 公钥认证设置（推荐）:
# 1. 在本地生成密钥对: ssh-keygen -t rsa -b 4096
# 2. 将公钥添加到堡垒机: ssh-copy-id user@bastion-ip
# 3. 或手动添加到 ~/.ssh/authorized_keys